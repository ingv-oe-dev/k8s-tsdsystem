apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tsdsystem-db
  labels:
    app: db
spec:
  template:
    spec:
      containers:
        - name: tsd-db
          resources:
            requests:
              cpu: "2"
              memory: "1Gi"
            limits:
              cpu: "4"
              memory: "2Gi"
